import React from 'react';
import {
    Text,
    View
} from 'react-native';

export default function DashboardScreen() {
  // content omitted for brevity (kept from original)
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Dashboard placeholder</Text>
    </View>
  );
}
